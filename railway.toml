[deploy]
startCommand = "php artisan config:cache && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
healthcheckPath = "/up"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
